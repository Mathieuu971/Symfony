{% extends "base.html.twig" %}
{% block body %}
    <p>La 2 <sup>ièm</sup> valeur du tableau : {{ tableau[1] }}</p>

    <p>Toutes les valeurs du tableau : </p>
        <ul>
            {% for i in 0..4 %}
                <li> {{ i ~ " : " ~ tableau[i] }} </li>
            {% endfor %}
        </ul>
        <p>Pour connaitre la taille d'un tableau, on utilise la fonction twig</p><code>. C'est un filtre : une fonction Twig qui s'utilise avec une syntaxe particuliere</code>
            <code>valeur|filtre</code>
            <ul>
                {% for i in 0..tableau|length - 1 %}
                    <li> {{ i ~ " : " ~ tableau[i] }} </li>
                {% endfor %}
            </ul>
            <p>Pour parcourir un tableau, on peut faire une boucle FOREACH</p>
            <table border="1">
                {% for valeur in tableau %}
                <tr>
                    <td>{{ valeur }}</td>
                </tr>
                {% endfor %}
            </table>
                   
                <p>Si on veut aussi récupérer les indices :</p>
                <table border="1">
                {% for indice, valeur in tableau %}
                    <tr>
                    <td>{{ indice }}</td>
                    <td>{{ valeur }}</td>
                    </tr>
                {% endfor %}

            </table>
{% endblock %}